# Shows a "Sponsor" button at the top of the repository.
# Replace the placeholder with your GitHub username once GitHub Sponsors is enabled
# (Settings -> Sponsors, then the Stripe Connect onboarding).
github: [panghea]

# Add later if you open one. Leave commented out until there is something to deliver monthly -
# an empty Patreon reads worse than no Patreon.
# patreon: YOUR-PATREON
